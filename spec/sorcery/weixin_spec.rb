require "spec_helper"

RSpec.describe Sorcery::Weixin do
  it "has a version number" do
    expect(Sorcery::Weixin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
